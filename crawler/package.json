{
  "name": "vaccine-news-crawler",
  "version": "1.0.0",
  "private": true,
  "description": "Scheduled crawler — fetches vaccine news from WHO/CDC/Google News and writes pending posts to Firestore.",
  "scripts": {
    "start": "ts-node index.ts",
    "crawl": "ts-node index.ts"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "rss-parser": "^3.13.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
